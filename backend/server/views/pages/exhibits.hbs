{{> head}}
<body class="page">
  {{> header}}

  <main class="main">
    <section class="section">
      <h2 class="section__title">Экспонаты</h2>
      <div class="exhibits-list">
        {{#each exhibits}}
          {{> exhibitCard}}
        {{/each}}
      </div>
    </section>
  </main>

  {{> footer}}
