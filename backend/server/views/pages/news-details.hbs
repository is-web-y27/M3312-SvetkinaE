{{> head}}
<body class="page">
  {{> header}}

  <main class="main">
    <section class="section">
      <h2 class="section__title">{{item.title}}</h2>
      <p>{{item.text}}</p>
      <p>Дата: {{item.publishedAt}}</p>
      {{#if item.exhibit}}
        <p>Экспонат: {{item.exhibit.title}}</p>
      {{/if}}
      <p>
        <a href="/news/{{item.id}}/edit{{authQ isAuth}}">Редактировать</a>
      </p>
      <form method="post" action="/news/{{item.id}}/delete{{authQ isAuth}}">
        <button type="submit">Удалить</button>
      </form>
      <p><a href="/news{{authQ isAuth}}">Назад к списку</a></p>
    </section>
  </main>

  {{> footer}}
