{{> head}}
<body class="page">
  {{> header}}

  <main class="main">
    <section class="section">
      <h2 class="section__title">Отзыв #{{item.id}}</h2>
      <p>Оценка: {{item.rating}}/5</p>
      <p>{{item.text}}</p>
      <p>Посетитель: {{item.visitor.name}}</p>
      <p>Экспонат: {{item.exhibit.title}}</p>
      {{#if isAuth}}
      <p>
        <a href="/reviews/{{item.id}}/edit">Редактировать</a>
      </p>
      <form method="post" action="/reviews/{{item.id}}/delete">
        <button type="submit">Удалить</button>
      </form>
      {{/if}}
      <p><a href="/reviews">Назад к списку</a></p>
    </section>
  </main>

  {{> footer}}
